// Brutalist landing page — replaces the previous template-feel hero.
// Logic-free wrapper so router/lazy-loading still work.
import BrutalistLanding from "./BrutalistLanding";

export default function Index() {
  return <BrutalistLanding />;
}
