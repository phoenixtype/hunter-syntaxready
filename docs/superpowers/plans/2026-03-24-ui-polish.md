# UI Polish — Approach C Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Apply a full consistency pass (Typography + Depth + Motion) across all three portals — candidate, recruiter, admin — keeping the existing blue `#1a73e8` primary colour and making no layout changes.

**Architecture:** All changes are purely presentational — Tailwind class updates, CSS additions, and one new skeleton component. No business logic, data fetching, or routing changes. Each task is independently safe to apply.

**Tech Stack:** React 18, TypeScript, Tailwind CSS v3, shadcn/ui (CVA), `tailwindcss-animate`, CSS custom properties

---

## File Map

| File | Action | Responsible for |
|------|--------|-----------------|
| `tailwind.config.ts` | Modify | Add `shadow-card`, `shadow-dropdown`, `shadow-modal` tokens; add `shimmer` keyframe + animation |
| `src/index.css` | Modify | Add scoped h1/h2/h3 scale; remove dead `.nav-active` + `.nav-active::before` CSS; remove `[cmdk-input]` focus override |
| `src/components/ui/card.tsx` | Modify | Add `shadow-card` default shadow; `rounded-xl` on base Card; hover lift |
| `src/components/ui/button.tsx` | Modify | Add `active:scale-[0.98] transition-transform duration-75` micro-press |
| `src/components/ui/skeleton-card.tsx` | Create | Reusable single stat card skeleton with shimmer (plain divs, not shadcn Skeleton) |
| `src/components/EmptyState.tsx` | Modify | Reduce icon container from `w-16 h-16` to `w-10 h-10` |
| `src/layouts/AppLayout.tsx` | Modify | Add `app-content` class to inner content wrapper |
| `src/layouts/AdminLayout.tsx` | Modify | Add `app-content` class to inner content wrapper |
| `src/layouts/RecruiterLayout.tsx` | Modify | Add `app-content` class to inner content wrapper |
| `src/components/AppSidebar.tsx` | Modify | Align active state to `bg-primary/10 text-primary font-semibold`, standardise padding |
| `src/components/recruiter/RecruiterSidebar.tsx` | Modify | Same active state alignment (nav items + company profile button) |
| `src/components/admin/AdminSidebar.tsx` | Modify | `font-medium` → `font-semibold` on active; standardise padding |
| `src/components/CommandPalette.tsx` (or equivalent) | Modify | Add `focus-visible:ring-2 focus-visible:ring-primary/50 focus-visible:ring-offset-2` on cmdk input |
| `src/components/DashboardWelcome.tsx` | Modify | Bold numerals, letter-spacing |
| `src/pages/admin/AdminOverview.tsx` | Modify | Replace `?? '—'` dash placeholders with skeleton grid while loading |
| `src/pages/admin/AdminReferrals.tsx` | Modify | Replace `<Loader2>` full-page spinner with skeleton layout |
| `src/pages/TailoredResumes.tsx` | Modify | Add `<EmptyState>` for empty list |
| `src/pages/recruiter/JobApplicants.tsx` | Modify | Add `<EmptyState>` for empty applicant list |
| `src/pages/admin/AdminUsers.tsx` | Modify | Add `<EmptyState>` for empty user table |
| `src/pages/AutoApplierSettings.tsx` | Modify | Add `<EmptyState>` for empty job list |

---

## Task 1: Add shadow tokens and shimmer animation to Tailwind config

**Files:**
- Modify: `tailwind.config.ts`

- [ ] **Step 1: Add three new shadow tokens inside `boxShadow` (after the last `"md-5"` entry)**

```ts
card:     '0 1px 3px rgba(0,0,0,0.07), 0 1px 2px rgba(0,0,0,0.04)',
dropdown: '0 4px 6px -1px rgba(0,0,0,0.08), 0 2px 4px -1px rgba(0,0,0,0.04)',
modal:    '0 10px 25px -5px rgba(0,0,0,0.1), 0 8px 10px -6px rgba(0,0,0,0.04)',
```

- [ ] **Step 2: Add `shimmer` keyframe inside the `keyframes` block (after `"scale-in"`)**

```ts
shimmer: {
  '0%':   { backgroundPosition: '200% 0' },
  '100%': { backgroundPosition: '-200% 0' },
},
```

- [ ] **Step 3: Add `shimmer` animation inside the `animation` block (after `"scale-in"`)**

```ts
shimmer: 'shimmer 1.4s ease-in-out infinite',
```

- [ ] **Step 4: Verify build**

```bash
npm run build 2>&1 | tail -20
```
Expected: no errors.

- [ ] **Step 5: Commit**

```bash
git add tailwind.config.ts
git commit -m "feat(ui): add shadow-card/dropdown/modal tokens and shimmer animation"
```

---

## Task 2: Update `src/index.css` — typography scale, remove dead CSS, fix focus ring

**Files:**
- Modify: `src/index.css`

- [ ] **Step 1: Add scoped h1/h2/h3 typography scale at the end of the file**

```css
/* ── Scoped typography scale (does not affect Radix portals) ── */
.app-content h1, .page-content h1 {
  font-size: 1.75rem; font-weight: 800; letter-spacing: -0.03em; line-height: 1.2;
}
.app-content h2, .page-content h2 {
  font-size: 1.375rem; font-weight: 700; letter-spacing: -0.02em; line-height: 1.25;
}
.app-content h3, .page-content h3 {
  font-size: 1.125rem; font-weight: 600; letter-spacing: -0.01em; line-height: 1.3;
}
```

- [ ] **Step 2: Remove the `[cmdk-input]` focus override block**

Find and delete this entire block (lines 178–183):
```css
[cmdk-input]:focus,
[cmdk-input]:focus-visible {
  outline: none !important;
  box-shadow: none !important;
  --tw-ring-shadow: none !important;
}
```

- [ ] **Step 3: Remove dead `.nav-active` and `.nav-active::before` rules**

Run:
```bash
grep -n "nav-active" src/index.css
```

Delete every line that is part of those two rules (the `.nav-active { ... }` block and the `.nav-active::before { ... }` block). The rules will look like:
```css
.nav-active {
  /* ... */
}
.nav-active::before {
  /* ... */
}
```
Delete both complete blocks.

- [ ] **Step 4: Verify build**

```bash
npm run build 2>&1 | tail -10
```

- [ ] **Step 5: Commit**

```bash
git add src/index.css
git commit -m "feat(ui): scoped typography scale, remove dead nav-active CSS and cmdk focus override"
```

---

## Task 3: Fix CommandPalette cmdk input focus ring

**Files:**
- Modify: the component that renders the command palette input (check with grep)

- [ ] **Step 1: Find the cmdk input component**

```bash
grep -rn "cmdk-input\|CommandInput\|<Command" src/components --include="*.tsx" -l
```

- [ ] **Step 2: Add focus ring directly on the input element**

Find the `<CommandInput>` (or the element with `cmdk-input`) and add to its `className`:
```
focus-visible:ring-2 focus-visible:ring-primary/50 focus-visible:ring-offset-2
```

- [ ] **Step 3: Verify build**

```bash
npm run build 2>&1 | tail -10
```

- [ ] **Step 4: Commit**

```bash
git add $(grep -rn "cmdk-input\|CommandInput" src/components --include="*.tsx" -l)
git commit -m "feat(ui): restore focus ring on CommandPalette input"
```

---

## Task 4: Add `app-content` class to all three layout wrappers

**Files:**
- Modify: `src/layouts/AppLayout.tsx`
- Modify: `src/layouts/AdminLayout.tsx`
- Modify: `src/layouts/RecruiterLayout.tsx`

- [ ] **Step 1: Update `AppLayout.tsx` (line 17)**

Find:
```tsx
<div className="flex-1 min-w-0 flex flex-col">
```
Replace with:
```tsx
<div className="flex-1 min-w-0 flex flex-col app-content">
```

- [ ] **Step 2: Update `AdminLayout.tsx` (line 6)**

Find:
```tsx
<div className="flex-1 min-w-0 flex flex-col">{children}</div>
```
Replace with:
```tsx
<div className="flex-1 min-w-0 flex flex-col app-content">{children}</div>
```

- [ ] **Step 3: Update `RecruiterLayout.tsx` (line 10)**

Find:
```tsx
<div className="flex-1 min-w-0 flex flex-col">{children}</div>
```
Replace with:
```tsx
<div className="flex-1 min-w-0 flex flex-col app-content">{children}</div>
```

- [ ] **Step 4: Verify build**

```bash
npm run build 2>&1 | tail -10
```

- [ ] **Step 5: Commit**

```bash
git add src/layouts/AppLayout.tsx src/layouts/AdminLayout.tsx src/layouts/RecruiterLayout.tsx
git commit -m "feat(ui): add app-content class to layout wrappers for scoped typography"
```

---

## Task 5: Update `card.tsx` — add default shadow and hover lift

**Files:**
- Modify: `src/components/ui/card.tsx`

- [ ] **Step 1: Read the file first**

```bash
# The Card base className string starts with "rounded-md border bg-card text-card-foreground"
```

- [ ] **Step 2: Update the `Card` component base className**

Find:
```tsx
className={cn("rounded-md border bg-card text-card-foreground", className)}
```
Replace with:
```tsx
className={cn(
  "rounded-xl border bg-card text-card-foreground shadow-card",
  "hover:shadow-md hover:-translate-y-px transition-all duration-150",
  className
)}
```

- [ ] **Step 3: Update `CardTitle` weight**

Find (CardTitle):
```tsx
className={cn("text-2xl font-semibold leading-none tracking-tight", className)}
```
Replace with:
```tsx
className={cn("text-xl font-bold leading-none tracking-tight", className)}
```

- [ ] **Step 4: Verify build**

```bash
npm run build 2>&1 | tail -10
```

- [ ] **Step 5: Commit**

```bash
git add src/components/ui/card.tsx
git commit -m "feat(ui): card shadow-card default, rounded-xl, hover lift"
```

---

## Task 6: Add `active:scale-[0.98]` micro-press to Button

**Files:**
- Modify: `src/components/ui/button.tsx`

- [ ] **Step 1: Read the CVA base string — it begins with `"inline-flex items-center justify-center"`**

- [ ] **Step 2: Add `active:scale-[0.98] transition-transform duration-75` to the base string**

Find:
```
"inline-flex items-center justify-center gap-2 whitespace-nowrap rounded-md text-sm font-medium ring-offset-background transition-colors focus-visible:outline-none focus-visible:ring-1 focus-visible:ring-ring disabled:pointer-events-none disabled:opacity-50 [&_svg]:pointer-events-none [&_svg]:size-4 [&_svg]:shrink-0"
```
Replace with:
```
"inline-flex items-center justify-center gap-2 whitespace-nowrap rounded-md text-sm font-medium ring-offset-background transition-colors active:scale-[0.98] transition-transform duration-75 focus-visible:outline-none focus-visible:ring-1 focus-visible:ring-ring disabled:pointer-events-none disabled:opacity-50 [&_svg]:pointer-events-none [&_svg]:size-4 [&_svg]:shrink-0"
```

- [ ] **Step 3: Verify build**

```bash
npm run build 2>&1 | tail -10
```

- [ ] **Step 4: Commit**

```bash
git add src/components/ui/button.tsx
git commit -m "feat(ui): add active:scale micro-press to Button base"
```

---

## Task 7: Create `skeleton-card.tsx` reusable stat card skeleton

**Files:**
- Create: `src/components/ui/skeleton-card.tsx`

Note: Use plain `<div>` elements with shimmer gradient, NOT `<Skeleton>` from shadcn — the shadcn `<Skeleton>` has `animate-pulse bg-muted` baked in which conflicts with the shimmer animation.

- [ ] **Step 1: Create the file**

```tsx
import { cn } from "@/lib/utils";

interface SkeletonCardProps {
  className?: string;
}

const shimmerClass =
  "rounded-md bg-gradient-to-r from-muted via-muted/50 to-muted bg-[length:200%_100%] animate-shimmer";

/**
 * Single stat card skeleton — matches the shape of admin stat cards.
 * Uses shimmer animation. Uses plain divs to avoid shadcn Skeleton's
 * animate-pulse conflicting with animate-shimmer.
 */
export const SkeletonCard = ({ className }: SkeletonCardProps) => (
  <div
    className={cn(
      "rounded-xl border bg-card shadow-card p-5 flex flex-col gap-3",
      className
    )}
  >
    {/* Eyebrow label */}
    <div className={cn(shimmerClass, "h-3 w-2/5")} />
    {/* Stat number */}
    <div className={cn(shimmerClass, "h-8 w-1/3")} />
    {/* Sub-label */}
    <div className={cn(shimmerClass, "h-3 w-3/5")} />
  </div>
);
```

- [ ] **Step 2: Verify it compiles**

```bash
npm run build 2>&1 | tail -10
```

- [ ] **Step 3: Commit**

```bash
git add src/components/ui/skeleton-card.tsx
git commit -m "feat(ui): add SkeletonCard component for admin loading states"
```

---

## Task 8: Reduce EmptyState icon container size

**Files:**
- Modify: `src/components/EmptyState.tsx`

- [ ] **Step 1: Update icon container classes**

Find:
```tsx
<div className="w-16 h-16 rounded-full bg-muted flex items-center justify-center mb-4">
```
Replace with:
```tsx
<div className="w-10 h-10 rounded-full bg-muted flex items-center justify-center mb-4">
```

- [ ] **Step 2: Verify build**

```bash
npm run build 2>&1 | tail -10
```

- [ ] **Step 3: Commit**

```bash
git add src/components/EmptyState.tsx
git commit -m "feat(ui): reduce EmptyState icon container to 40px"
```

---

## Task 9: Align AppSidebar active state

**Files:**
- Modify: `src/components/AppSidebar.tsx`

The current active nav item class pattern is:
```
active ? "bg-muted text-foreground font-medium" : "text-muted-foreground hover:text-foreground hover:bg-muted/60 font-normal"
```

- [ ] **Step 1: Find all occurrences of the active state class pattern**

```bash
grep -n "bg-muted text-foreground font-medium" src/components/AppSidebar.tsx
```

Note how many occurrences exist (there are likely 2: one for NAV_ITEMS, one for tool sections).

- [ ] **Step 2: Replace ALL occurrences of the inactive/active pattern**

For each occurrence, replace:
```
active
  ? "bg-muted text-foreground font-medium"
  : "text-muted-foreground hover:text-foreground hover:bg-muted/60 font-normal"
```
With:
```
active
  ? "bg-primary/10 text-primary font-semibold"
  : "text-muted-foreground hover:text-foreground hover:bg-muted transition-colors duration-150 font-normal"
```

Use `replace_all: true` if the pattern is identical across all occurrences.

- [ ] **Step 3: Standardise padding — find all collapsed ternary padding strings**

```bash
grep -n "px-2.5 py-1.5" src/components/AppSidebar.tsx
```

For each match, replace `"px-2.5 py-1.5"` with `"px-3 py-2"` (use `replace_all: true`).

- [ ] **Step 4: Verify build**

```bash
npm run build 2>&1 | tail -10
```

- [ ] **Step 5: Commit**

```bash
git add src/components/AppSidebar.tsx
git commit -m "feat(ui): align AppSidebar active state to bg-primary/10 text-primary"
```

---

## Task 10: Align RecruiterSidebar active state

**Files:**
- Modify: `src/components/recruiter/RecruiterSidebar.tsx`

- [ ] **Step 1: Find ALL active state occurrences (nav items + company profile button)**

```bash
grep -n "bg-muted text-foreground font-medium" src/components/recruiter/RecruiterSidebar.tsx
```

There are at least 2 occurrences: the `NAV_ITEMS.map()` loop and the Company Profile nav button.

- [ ] **Step 2: Replace all occurrences**

Use `replace_all: true` on:
```
active ? "bg-muted text-foreground font-medium" : "text-muted-foreground hover:text-foreground hover:bg-muted/60 font-normal"
```
Replace with:
```
active ? "bg-primary/10 text-primary font-semibold" : "text-muted-foreground hover:text-foreground hover:bg-muted transition-colors duration-150 font-normal"
```

- [ ] **Step 3: Fix the Company Profile button active state separately**

The Company Profile button uses `pathname === "/recruiter/company"` as its condition (not `active`), so the `replace_all` in Step 2 will not match it. Apply a separate targeted Edit:

Find:
```
pathname === "/recruiter/company" ? "bg-muted text-foreground font-medium" : "text-muted-foreground hover:text-foreground hover:bg-muted/60 font-normal"
```
Replace with:
```
pathname === "/recruiter/company" ? "bg-primary/10 text-primary font-semibold" : "text-muted-foreground hover:text-foreground hover:bg-muted transition-colors duration-150 font-normal"
```

- [ ] **Step 4: Standardise padding**

```bash
grep -n "px-2.5 py-1.5" src/components/recruiter/RecruiterSidebar.tsx
```

Replace all `"px-2.5 py-1.5"` occurrences with `"px-3 py-2"`.

- [ ] **Step 5: Verify build**

```bash
npm run build 2>&1 | tail -10
```

- [ ] **Step 6: Commit**

```bash
git add src/components/recruiter/RecruiterSidebar.tsx
git commit -m "feat(ui): align RecruiterSidebar active state to bg-primary/10 text-primary"
```

---

## Task 11: Update AdminSidebar — semibold active, standardise padding

**Files:**
- Modify: `src/components/admin/AdminSidebar.tsx`

- [ ] **Step 1: Bump active font-weight (line 47)**

Find:
```
'bg-primary/10 text-primary font-medium'
```
Replace with:
```
'bg-primary/10 text-primary font-semibold'
```

- [ ] **Step 2: Standardise nav item padding + rounded-lg (line 45)**

Find:
```
'flex items-center gap-2.5 px-2.5 py-2 rounded-md text-sm transition-colors',
```
Replace with:
```
'flex items-center gap-2.5 px-3 py-2 rounded-lg text-sm transition-colors',
```

- [ ] **Step 3: Standardise sign-out button padding**

Find:
```
"flex items-center gap-2.5 px-2.5 py-2 rounded-md text-sm text-muted-foreground hover:bg-muted hover:text-foreground transition-colors w-full text-left"
```
Replace with:
```
"flex items-center gap-2.5 px-3 py-2 rounded-lg text-sm text-muted-foreground hover:bg-muted hover:text-foreground transition-colors w-full text-left"
```

- [ ] **Step 4: Verify build**

```bash
npm run build 2>&1 | tail -10
```

- [ ] **Step 5: Commit**

```bash
git add src/components/admin/AdminSidebar.tsx
git commit -m "feat(ui): AdminSidebar font-semibold active, standardise padding, rounded-lg"
```

---

## Task 12: Shadow replacement sweep

**Files:**
- All card/panel usages across candidate, recruiter, admin portals

- [ ] **Step 1: Find all `shadow-sm` usages in src/**

```bash
grep -rn "shadow-sm" src/ --include="*.tsx" --include="*.ts"
```

For each file that uses `shadow-sm` on a card or panel (not a navbar or input), replace `shadow-sm` with `shadow-card`.

- [ ] **Step 2: Find all `shadow-lg` usages**

```bash
grep -rn "shadow-lg" src/ --include="*.tsx" --include="*.ts"
```

For each file that uses `shadow-lg` on a dialog, sheet, or modal (not a logo icon), replace `shadow-lg` with `shadow-modal`.

- [ ] **Step 3: Keep `shadow-md-1` on the top navbar only (already correct)**

No change needed for `shadow-md-1` usages.

- [ ] **Step 4: Verify build**

```bash
npm run build 2>&1 | tail -10
```

- [ ] **Step 5: Commit**

```bash
git add -p  # Stage the relevant files
git commit -m "feat(ui): replace shadow-sm→shadow-card and shadow-lg→shadow-modal across portals"
```

---

## Task 13: Stat numerals polish in DashboardWelcome

**Files:**
- Modify: `src/components/DashboardWelcome.tsx`

- [ ] **Step 1: Update metrics row numerals to bold + tight tracking**

Find:
```tsx
<span><strong className="text-foreground font-semibold">{appCount}</strong> applied</span>
<span><strong className="text-foreground font-semibold">{metrics.interviews}</strong> interviews</span>
<span><strong className="text-foreground font-semibold">{metrics.offers}</strong> offers</span>
```
Replace with:
```tsx
<span><strong className="text-foreground font-extrabold tracking-tight tabular-nums">{appCount}</strong> applied</span>
<span><strong className="text-foreground font-extrabold tracking-tight tabular-nums">{metrics.interviews}</strong> interviews</span>
<span><strong className="text-foreground font-extrabold tracking-tight tabular-nums">{metrics.offers}</strong> offers</span>
```

Note: No delta/percentage indicators exist in the current `DashboardWelcome` or `AdminOverview` code — the spec's delta badge treatment (↑ 12%) is a progressive enhancement to apply if/when delta values are added to these components in future. Skip for now.

- [ ] **Step 2: Verify build**

```bash
npm run build 2>&1 | tail -10
```

- [ ] **Step 3: Commit**

```bash
git add src/components/DashboardWelcome.tsx
git commit -m "feat(ui): bold tight numerals in DashboardWelcome metrics row"
```

---

## Task 14: Add loading skeleton to AdminOverview

**Files:**
- Modify: `src/pages/admin/AdminOverview.tsx`

- [ ] **Step 1: Add SkeletonCard import**

```tsx
import { SkeletonCard } from '@/components/ui/skeleton-card';
```

- [ ] **Step 2: Add a `!stats` loading guard before the main return**

After the `if (error)` block (around line 55), add:
```tsx
if (!stats) {
  return (
    <>
      <SEOHead title="Admin Overview | Hunter" />
      <div className="p-6 max-w-5xl mx-auto w-full">
        <div className="h-7 w-40 bg-muted animate-pulse rounded-md mb-6" />
        <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
          <SkeletonCard />
          <SkeletonCard />
          <SkeletonCard />
          <SkeletonCard />
          <SkeletonCard />
          <SkeletonCard />
        </div>
      </div>
    </>
  );
}
```

- [ ] **Step 3: Verify the component no longer renders `?? '—'` dash placeholders while loading**

After adding the guard, the main return only runs when `stats` is defined, so `stats.totalUsers` (not `stats?.totalUsers ?? '—'`) can be used safely.

- [ ] **Step 4: Verify build**

```bash
npm run build 2>&1 | tail -10
```

- [ ] **Step 5: Commit**

```bash
git add src/pages/admin/AdminOverview.tsx
git commit -m "feat(ui): replace dash placeholders with SkeletonCard grid in AdminOverview"
```

---

## Task 15: Replace Loader2 spinner in AdminReferrals with skeleton

**Files:**
- Modify: `src/pages/admin/AdminReferrals.tsx`

- [ ] **Step 1: Add SkeletonCard import**

```tsx
import { SkeletonCard } from '@/components/ui/skeleton-card';
```

- [ ] **Step 2: Find the loading spinner block**

```bash
grep -n "Loader2\|loading\|spinner" src/pages/admin/AdminReferrals.tsx | head -20
```

- [ ] **Step 3: Replace the `<Loader2>` spinner block**

Find the loading render path (the block that shows a centred `<Loader2>`). Replace it with:
```tsx
if (loading) {
  return (
    <div className="p-6 max-w-5xl mx-auto w-full space-y-6">
      <div className="grid grid-cols-1 sm:grid-cols-2 gap-4">
        <SkeletonCard />
        <SkeletonCard />
      </div>
      <div className="rounded-xl border bg-card shadow-card p-5 space-y-3">
        <div className="h-3 w-1/4 bg-muted animate-pulse rounded-md" />
        <div className="h-3 w-full bg-muted animate-pulse rounded-md" />
        <div className="h-3 w-3/4 bg-muted animate-pulse rounded-md" />
        <div className="h-3 w-5/6 bg-muted animate-pulse rounded-md" />
      </div>
    </div>
  );
}
```

- [ ] **Step 4: Remove unused `Loader2` import**

```bash
grep -n "Loader2" src/pages/admin/AdminReferrals.tsx
```

If it only appears in the now-removed block, delete it from the import line.

- [ ] **Step 5: Verify build**

```bash
npm run type-check 2>&1 | tail -20
```

- [ ] **Step 6: Commit**

```bash
git add src/pages/admin/AdminReferrals.tsx
git commit -m "feat(ui): replace full-page spinner with skeleton in AdminReferrals"
```

---

## Task 16: Add EmptyState to TailoredResumes

**Files:**
- Modify: `src/pages/TailoredResumes.tsx`

- [ ] **Step 1: Check current empty handling**

```bash
grep -n "empty\|length === 0\|\.length\|No " src/pages/TailoredResumes.tsx | head -20
```

- [ ] **Step 2: Add imports**

```tsx
import { EmptyState } from '@/components/EmptyState';
import { FolderOpen } from 'lucide-react';
```

(Check if `FolderOpen` is already imported before adding.)

- [ ] **Step 3: Replace the empty list bare text/null with EmptyState**

```tsx
<EmptyState
  icon={<FolderOpen className="w-5 h-5 text-muted-foreground" />}
  title="No tailored resumes yet"
  description="Generate a tailored resume for a specific job to see it here."
/>
```

- [ ] **Step 4: Verify build**

```bash
npm run build 2>&1 | tail -10
```

- [ ] **Step 5: Commit**

```bash
git add src/pages/TailoredResumes.tsx
git commit -m "feat(ui): add EmptyState to TailoredResumes empty list"
```

---

## Task 17: Add EmptyState to JobApplicants

**Files:**
- Modify: `src/pages/recruiter/JobApplicants.tsx`

- [ ] **Step 1: Check current empty handling**

```bash
grep -n "empty\|length === 0\|applicants\|No " src/pages/recruiter/JobApplicants.tsx | head -20
```

- [ ] **Step 2: Add imports (check if `Users` is already imported)**

```tsx
import { EmptyState } from '@/components/EmptyState';
// import { Users } from 'lucide-react'; // add only if not already imported
```

- [ ] **Step 3: Replace empty applicants condition**

```tsx
<EmptyState
  icon={<Users className="w-5 h-5 text-muted-foreground" />}
  title="No applicants yet"
  description="Candidates who apply to this job will appear here."
/>
```

- [ ] **Step 4: Verify build**

```bash
npm run build 2>&1 | tail -10
```

- [ ] **Step 5: Commit**

```bash
git add src/pages/recruiter/JobApplicants.tsx
git commit -m "feat(ui): add EmptyState to JobApplicants empty list"
```

---

## Task 18: Add EmptyState to AdminUsers

**Files:**
- Modify: `src/pages/admin/AdminUsers.tsx`

- [ ] **Step 1: Check current empty handling**

```bash
grep -n "empty\|length === 0\|users\|No " src/pages/admin/AdminUsers.tsx | head -20
```

- [ ] **Step 2: Add imports**

```tsx
import { EmptyState } from '@/components/EmptyState';
// import { Users } from 'lucide-react'; // add only if not already imported
```

- [ ] **Step 3: Replace empty table bare text with EmptyState**

```tsx
<EmptyState
  icon={<Users className="w-5 h-5 text-muted-foreground" />}
  title="No users found"
  description="No users match your current search or filter criteria."
/>
```

- [ ] **Step 4: Verify build**

```bash
npm run build 2>&1 | tail -10
```

- [ ] **Step 5: Commit**

```bash
git add src/pages/admin/AdminUsers.tsx
git commit -m "feat(ui): add EmptyState to AdminUsers empty table"
```

---

## Task 19: Add EmptyState to AutoApplierSettings job list

**Files:**
- Modify: `src/pages/AutoApplierSettings.tsx`

- [ ] **Step 1: Check current empty handling**

```bash
grep -n "empty\|length === 0\|jobs\|No " src/pages/AutoApplierSettings.tsx | head -20
```

- [ ] **Step 2: Add imports**

```tsx
import { EmptyState } from '@/components/EmptyState';
import { Bot } from 'lucide-react';
```

(Check if `Bot` is already imported before adding.)

- [ ] **Step 3: Replace the empty job list bare text/null with EmptyState**

```tsx
<EmptyState
  icon={<Bot className="w-5 h-5 text-muted-foreground" />}
  title="No jobs queued"
  description="Jobs matching your preferences will appear here when the auto-applier finds them."
/>
```

- [ ] **Step 4: Verify build**

```bash
npm run build 2>&1 | tail -10
```

- [ ] **Step 5: Commit**

```bash
git add src/pages/AutoApplierSettings.tsx
git commit -m "feat(ui): add EmptyState to AutoApplierSettings empty job list"
```

---

## Task 20: Breadcrumb separator + page title standardisation (Spec §3 + §5)

**Files:**
- All page components that render breadcrumbs and page titles

- [ ] **Step 1: Find all breadcrumb separator occurrences**

```bash
grep -rn '"/"\|breadcrumb.*/' src/ --include="*.tsx" | grep -v node_modules | head -30
```

Also search for the PageHeader component used across pages:
```bash
grep -rn "PageHeader\|breadcrumb\|Breadcrumb" src/ --include="*.tsx" -l
```

- [ ] **Step 2: Replace all `/` breadcrumb separators with `›`**

For each file that renders a breadcrumb separator as a literal `/` character, replace it with:
```tsx
<span className="text-muted-foreground/40 text-xs">›</span>
```

- [ ] **Step 3: Survey page title weights**

```bash
grep -rn "text-xl\|text-2xl\|text-lg" src/pages --include="*.tsx" | grep -i "font-bold\|font-semibold\|title" | head -30
```

For any page title `<h1>` or title `<p>`/`<div>` that is not `text-2xl font-bold tracking-tight`, update it to:
```tsx
className="text-2xl font-bold tracking-tight"
```

- [ ] **Step 4: Add subtitle where missing**

For pages that have a title but no subtitle `<p className="text-sm text-muted-foreground">`, add one directly below the title.

- [ ] **Step 5: Verify build**

```bash
npm run build 2>&1 | tail -10
```

- [ ] **Step 6: Commit**

```bash
git add -p  # Stage only the pages/components changed
git commit -m "feat(ui): breadcrumb separator › and page title tracking-tight standardisation"
```

---

---

## Task 21: Final verification — full build + type-check

- [ ] **Step 1: Full build**

```bash
npm run build
```
Expected: no errors.

- [ ] **Step 2: Lint check**

```bash
npm run lint 2>&1 | tail -20
```

- [ ] **Step 3: Type-check**

```bash
npm run type-check
```

- [ ] **Step 4: Visual smoke-test**

```bash
npm run dev
```

Check:
- All three sidebars: active item has blue `bg-primary/10` background, not grey `bg-muted`
- Cards have subtle shadow and 1px lift on hover
- Button has micro-press feel on click
- AdminOverview shows skeleton grid on first load (not dashes)
- AdminReferrals shows skeleton, not full-page spinner
- TailoredResumes / JobApplicants / AdminUsers / AutoApplierSettings show EmptyState on empty data

---

## Summary of Commits Expected (21 tasks → 21 commits)

1. `feat(ui): add shadow-card/dropdown/modal tokens and shimmer animation`
2. `feat(ui): scoped typography scale, remove dead nav-active CSS and cmdk focus override`
3. `feat(ui): restore focus ring on CommandPalette input`
4. `feat(ui): add app-content class to layout wrappers for scoped typography`
5. `feat(ui): card shadow-card default, rounded-xl, hover lift`
6. `feat(ui): add active:scale micro-press to Button base`
7. `feat(ui): add SkeletonCard component for admin loading states`
8. `feat(ui): reduce EmptyState icon container to 40px`
9. `feat(ui): align AppSidebar active state to bg-primary/10 text-primary`
10. `feat(ui): align RecruiterSidebar active state to bg-primary/10 text-primary`
11. `feat(ui): AdminSidebar font-semibold active, standardise padding, rounded-lg`
12. `feat(ui): replace shadow-sm→shadow-card and shadow-lg→shadow-modal across portals`
13. `feat(ui): bold tight numerals in DashboardWelcome metrics row`
14. `feat(ui): replace dash placeholders with SkeletonCard grid in AdminOverview`
15. `feat(ui): replace full-page spinner with skeleton in AdminReferrals`
16. `feat(ui): add EmptyState to TailoredResumes empty list`
17. `feat(ui): add EmptyState to JobApplicants empty list`
18. `feat(ui): add EmptyState to AdminUsers empty table`
19. `feat(ui): add EmptyState to AutoApplierSettings empty job list`
20. `feat(ui): breadcrumb separator › and page title tracking-tight standardisation`
21. Final verification (no commit)
